package edu.uprm.capstone.icom5047.btcomm;

import android.bluetooth.BluetoothDevice;

import java.util.UUID;

/**
 * Created by Kenneth on 4/3/2016.
 */
public class ConnectManager {

    BluetoothDevice blueDevice;
    UUID uuid;


    public ConnectManager(String id) {
        //ConnectThread thread = new ConnectThread(blueDevice.getName().equals(id), uuid);

    }
}
